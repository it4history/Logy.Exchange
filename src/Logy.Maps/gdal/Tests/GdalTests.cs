#if DEBUG
/*using NUnit.Framework;
using OSGeo.GDAL;

namespace Logy.Maps.gdal.Tests
{
    [TestFixture]
    public class GdalTests
    {
        [Test]
        public void FileFromMemBuffer()
        {
            GdalConfiguration.ConfigureGdal();
            Gdal.FileFromMemBuffer("test", new byte[100]);
            var ds = Gdal.Open("test", Access.GA_Update);
            //var image = ds.GetRasterBand(1).ReadAsArray();
            Gdal.Unlink("test");
        }
    }
}*/
#endif
